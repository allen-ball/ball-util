<?xml version="1.0" encoding="UTF-8"?>
<!-- $Id$ -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ball</groupId>
    <artifactId>parent</artifactId>
    <version>1.0</version>
    <relativePath>../ball/parent/pom.xml</relativePath>
  </parent>
  <groupId>ball</groupId>
  <artifactId>ball-util</artifactId>
  <name>Utilities</name>
  <packaging>jar</packaging>
  <properties>
    <jre.version>1.6</jre.version>
  </properties>
  <profiles>
    <profile>
      <id>javadoc</id>
      <activation>
        <file><exists>${basedir}/src/main/javadoc</exists></file>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <configuration>
              <tagletpath>${project.build.outputDirectory}:${dependency.runtime.classpath}</tagletpath>
              <tagletArtifacts combine.self="override"/>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <dependencies verbose="true">
    <dependency>
      <groupId>java</groupId>
      <artifactId>jre</artifactId>
      <version>${jre.version}</version>
    </dependency>
    <dependency>
      <groupId>com.sun</groupId>
      <artifactId>tools</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.apache.ant</groupId>
      <artifactId>ant</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-core</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
  </dependencies>
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>ball</groupId>
          <artifactId>ball-artifact-maven-plugin</artifactId>
          <executions>
            <execution>
              <id>attach-shared-library</id>
              <goals>
                <goal>attach</goal>
              </goals>
              <configuration>
                <type>dylib</type>
                <classifier>${os.name}-${os.arch}</classifier>
                <file>
                  ${project.build.directory}/lib${project.artifactId}.dylib
                </file>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>

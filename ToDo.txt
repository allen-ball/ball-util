$Id$

* build.xml: Remove magic "darwin" from <libtool-compile/>.

* CoordinateMap: Calculate getColumnClass(int) (type member) by determining
  parameter V through reflection.

* Reimplement IOUtil.copy(FileChannel,FileChannel) so it respects and updates
  the FileChannels' positions.

* Add checks to iprotium.tools or iprotium.annotation.processing:

        ** Check arguments to IOUtil.close(Object) are Closeable,
           IOUtil.flush(Object) are Flushable, etc.
        ** CollectionToArrayProcessor: Finish implementation.
        ** ConstantInitializerProcessor: Finish implementation.
        ** ConstructorProcessor: Require explicitly declared constructors
        ** Suggest (! Collection.isEmpty()) for (Collection.size() > 0)
        ** ByteArrayOutputStream: Prefer ByteArrayOutputStream(int)
           constructor with an argument > 32

* Create SSU package.


Local Variables:
mode: auto-fill
fill-column: 78
End:

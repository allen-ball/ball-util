$Id: ToDo.txt,v 1.9 2010-07-16 18:14:56 ball Exp $

* Add JNI interface to editline(libedit)/readline.

* Add checks to LintTask:

        ** Require explicitly declared constructors
        ** Require non-public constructors for abstract classes
        ** Require @Override annotations on overridden methods
        ** Suggest (! Collection.isEmpty()) for (Collection.size() > 0)
        ** ByteArrayOutputStream: Prefer ByteArrayOutputStream(int)
           constructor with an argument > 32
        ** Require Cloneable.clone() implementations override clone() to
           return the implementing type

* Create SSU package.


Local Variables:
mode: auto-fill
fill-column: 78
End:
